/** Live DOS Workspace (demo / client hub) — embedded on `/workspace-demo` with framing banner. */
export const DOS_WORKSPACE_LIVE_URL = "https://command.directiveos.com.au/os/dashboard";

/** In-app route: banner + iframe wrapper around {@link DOS_WORKSPACE_LIVE_URL}. */
export const DOS_WORKSPACE_DEMO_PATH = "/workspace-demo" as const;
